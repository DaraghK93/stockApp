
import { Button } from 'react-bootstrap';

function FollowButton() {
    return (
        <Button variant="secondary" className="followButton btn btn-secondary btn-sm">Follow</Button>
    );
}

export default FollowButton;
